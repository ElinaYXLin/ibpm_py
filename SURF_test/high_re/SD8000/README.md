# SD8000 airfoil vs. UIUC LSAT experiment

Same methodology as [`SURF_test/SD7003/README.md`](../SD7003/README.md) —
read that first for the full setup/limitations discussion, **and for the
full explanation of the `2-c++included/`/`1-orig/` split and what the removed error-bar
whiskers meant** (same explanation applies here verbatim, not repeated).
This is the second of two real cambered-airfoil validation cases (see also
`SURF_test/SD7003/`), generated by the same scripts
([`gen_airfoil_report.py`](../SD7003/gen_airfoil_report.py) /
[`gen_airfoil_report_v2.py`](../gen_airfoil_report_v2.py),
[`gen_flowfield_figs.py`](../SD7003/gen_flowfield_figs.py) /
[`gen_flowfield_figs_v2.py`](../gen_flowfield_figs_v2.py), all live in
`SURF_test/` or `SURF_test/SD7003/` and operate on both airfoils), plus
[`run_all_airfoils_cpp.py`](../run_all_airfoils_cpp.py) /
[`run_flowfield_cpp.py`](../run_flowfield_cpp.py) for the new C++ runs.

## `2-c++included/` vs. `1-orig/` vs. `3-small_dt/`

- **`2-c++included/`** — current figures: 3-way comparison (`py/ibpm.py` vs. C++
  `build/ibpm` vs. UIUC LSAT experiment), no error-bar whiskers.
- **`1-orig/`** — original 2-way, error-barred figures, kept for the record.
- **`3-small_dt/`** — small-timestep (dt=0.001) Python (native FFTW3) vs.
  C++ validation, generated by
  [`../run_small_dt_validation.py`](../run_small_dt_validation.py) /
  [`../gen_small_dt_report.py`](../gen_small_dt_report.py) — see
  `../SD7003/README.md`'s "Port now calls the real FFTW3 library" section
  for the full explanation (same methodology/result here: `|ΔCd|`/`|ΔCl|`
  never exceed 1e-18 and the vorticity/flux fields agree to
  floating-point-roundoff level throughout t=0-2.0).

## Data provenance (confirmed online)

- **Coordinates**: `SURF_test/SD8000/sd8000.dat.txt` (header
  `SD8000-089-88`) matches the file at
  `https://m-selig.ae.illinois.edu/ads/coord/sd8000.dat` (confirmed by
  fetching that URL directly).
- **Performance data**: `SD8000.DRG.txt`/`SD8000.LFT.txt` are in the
  same genuine UIUC LSAT tabulated format as SD7003's (see that
  README for the caveat on not locating the exact hosting volume for
  these specific tables).

## Setup

Identical to SD7003: `length=6, xoffset=-2, yoffset=-1.5`, `dt=0.01`
(`0.005` at the finest grid level), `Re=60800` (UIUC's lowest-Re bin),
boundary resampled to uniform arc-length spacing matched to each run's
`dx` (raw UIUC spacing is again far too non-uniform to use directly —
see SD7003's README for why).

## Files

Same set as `SURF_test/SD7003/`: `polar_comparison.png`, `drag_polar.png`,
`grid_convergence.png`, `flow_evolution.png`, `summary.txt` — each present
in both `2-c++included/` (3-way, no error bars) and `1-orig/` (2-way, error-barred), plus
`_run_data/` (Python) and `_run_data_cpp/` (C++, new) at the top level.

## Results

**Lift matches well, for both implementations,** across $\alpha=-3.9°$ to
$8.4°$ (e.g. at $\alpha=8.36°$: py $C_l=0.921$, cpp $C_l=0.889$, both vs.
exp $C_l=1.034$ — see `2-c++included/summary.txt`) — see `2-c++included/polar_comparison.png`.
Same qualitative story as SD7003, now corroborated by an independent
implementation.

**Drag is overpredicted at higher $\alpha$, by both implementations,** for
the same reasons as SD7003 (no transition model, under-resolved laminar
separation bubble physics at Re≈60800): at $\alpha=8.36°$, py
$C_d=0.147$, cpp $C_d=0.133$, both vs. exp $C_d=0.028$.

**Grid convergence (`2-c++included/grid_convergence.png`)**, at mild $\alpha=-0.81°$:
this case shows an even more dramatic convergence story than SD7003's, in
**both** implementations. `py/ibpm.py`'s coarsest grid (dx=0.04) gets the
*sign* of $C_l$ wrong (py $-0.149\pm1.22$ vs. exp $+0.065$); C++'s coarsest
grid is even more extreme — $+0.85\pm46.5$, i.e. the instantaneous force
trace swings wildly enough that the std. dev. is **~55x** the mean, direct
numerical evidence that dx=0.04 does not resolve this flow *at all* for
either implementation (this is exactly the kind of number a plotted error
bar would render meaningless/off-scale, and part of why `2-c++included/`'s figures
don't draw them — see `../SD7003/README.md`'s "About the error bars").
Both implementations' medium (dx=0.02) and fine (dx=0.01) grids recover the
correct sign and converge toward the experimental value, with the
unsteadiness collapsing sharply (py $C_l$ std: 1.22 → 0.31 → 0.04; cpp
$C_l$ std: 46.5 → 0.33 → 0.05, coarse to fine). This is the clearest
evidence in either airfoil case — now doubled by cross-implementation
agreement — that the coarse-grid disagreement is a genuine resolution
artifact rather than a modeling error. Full numbers in `2-c++included/summary.txt`.

**Same finest-grid CFL divergence** as SD7003 was hit here too, **in both
implementations** (dx=0.01 diverged to `NaN` at `dt=0.01`, fixed the same
way with `dt=0.005`) — see `SURF_test/rerun_fine_log.txt` (Python) and
`run_all_airfoils_cpp.py` (C++).

## Limitations

Identical caveat to SD7003: `2-c++included/flow_evolution.png` shows the same
broadband grid-scale noise spreading through the domain at this
Re/resolution, **in both `py/ibpm.py` and C++ `build/ibpm`** — trust the
integrated force/convergence numbers over the flow-field snapshots. See
`../SD7003/README.md`'s "Confirmed: `src/` ... is byte-identical to
upstream `cwrowley/ibpm`" section for why this noise is expected
behavior of the unmodified original solver in this configuration
(`ngrid=1`, Re≈60800), not a porting or formulation bug -- same
solver/algorithm, same explanation, applies verbatim here.

## Two follow-up questions (and why neither indicates a faulty port)

`2-c++included/port_fidelity_diagnostic.png`
([`../gen_port_fidelity_diagnostic.py`](../gen_port_fidelity_diagnostic.py))
answers two sharp questions directly from the committed `.force` traces.

**Q1. Why does SD8000 (aerodynamically the easier airfoil) show the same
chaotic vorticity as SD7003?** Because the broadband vorticity noise is a
property of **solver + resolution + Reynolds number, not airfoil shape**.
Both airfoils are run identically: Re≈60000, `ngrid=1` (single-domain,
least-accurate far-field the method supports), dx=0.02, 2D laminar, no
transition model, no subgrid dissipation. The grid-scale noise comes from
the immersed-boundary delta-function regularization + explicit convection
being under-resolved at this Re — that happens for *any* body in this
configuration. "Easy" describes SD8000's aerodynamics (clean, attached,
well-behaved integrated lift), not its numerical resolvability. So SD7003
is not uniquely "the problem"; both airfoils hit the same wall, which is
exactly why the caveat is identical for both.

**Q2. At the coarse grid (dx=0.04) the SD8000 time-averaged $C_l$ for
Python (−0.15) and C++ (+0.85) have opposite signs and are both far from
experiment (+0.065). Is the port faulty?** No — and the "opposite signs"
is largely a mirage:

- **The coarse grid is numerically unstable.** The C++ trace ends in a
  single-step **blow-up to $C_l$ ≈ 1973** at the very last timestep
  (panel A). That one spike is what drags C++'s windowed mean to +0.85;
  drop it (1 of 1801 points) and C++'s mean is **−0.24 — the same sign as
  Python's −0.15.** They were never really "opposite"; a blow-up spike
  polluted the average.
- **Even without the spike, the mean is meaningless here:** at dx=0.04 the
  oscillation std is 8× (py) to 55× (cpp) the mean, i.e. the "mean $C_l$"
  is the residue of averaging a wildly-oscillating, near-unstable signal.
  Its exact value — and sign — is set by roundoff-level chaos, so two
  implementations landing in different places is *expected*, not a bug.
- **The decisive evidence the port is faithful:** on the **converged fine
  grid (dx=0.01)**, Python and C++ are **bit-for-bit identical**
  ($|C_l^{py}-C_l^{cpp}| = 0$ to every digit) for the **first ~1000
  timesteps** (panels B, C), and only then does the difference grow
  exponentially from the floating-point floor — the textbook signature of
  chaotic amplification of last-bit roundoff (from documented
  summation-order differences, e.g. `np.dot` vs. C++'s sequential sum in
  `cholesky_solver.py`), identical in mechanism to the vortall case
  (`../vortall/README.md`). A genuine porting bug (wrong operator, sign,
  or boundary condition) would disagree **from step 1 and at every
  resolution, including the converged fine grid** — instead the two codes
  agree exactly early, and their converged fine-grid means agree to ~6%
  (py +0.163, cpp +0.154). The port reproduces the reference so faithfully
  it even nearly blows up the same way on the unstable coarse grid.

Bottom line: the coarse-grid sign flip is an artifact of time-averaging an
unstable run, not a fault in the port; the port is validated to bit-level
fidelity wherever the underlying flow is numerically well-posed.
